type User {
  id: UUID!
  username: String!
  password: String!
  email: String!
  privilege: String!
  createdAt: String!
  updatedAt: String!
}

type Query {
  getUsers: [User!]
  getUser(at: UUID!): User!
}

input CreateUserInput {
  username: String!
  password: String!
  email: String!
  privilege: String!
  active: Boolean!
}

input LoginInput {
  email: String!
  password: String!
}

type AuthPayload {
  token: String!
}

type Mutation {
  createUser(input: CreateUserInput): User!
  login(input: LoginInput): AuthPayload!
}
